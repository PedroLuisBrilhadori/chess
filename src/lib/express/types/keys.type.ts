export enum Classes {
  Controller = "Controller",
}

export enum Requests {
  Post = "post",
  Get = "get",
}

export enum Params {
  Body = "body",
  Query = "query",
  Request = "request",
  Response = "Response",
}
